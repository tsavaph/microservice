spring:
  application:
    name: forex-service
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/forex
    username: admin
    password: admin

springdoc:
  swagger-ui:
    use-root-path: true
    enabled: true

server:
  port: 8000
